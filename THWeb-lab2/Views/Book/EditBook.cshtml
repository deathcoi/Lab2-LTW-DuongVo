@model THWeb_lab2.Models.Book

@{
    ViewBag.Title = "EditBook";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>EditBook</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Book</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)
    <table>
        <tr>
            <td>
                Title:
            </td>
            <td>
                @Html.TextBoxFor(model => model.Title, new { @class = "form-control", placehodel = "Title" })
            </td>
        </tr>
        <tr>
            <td>
                Image:
            </td>
            <td>
                @Html.TextBoxFor(model => model.Image, new { @class = "form-control", placehodel = "Image" })
            </td>
        </tr>
        <tr>
            <td>
                Author:
            </td>
            <td>
                @Html.TextBoxFor(model => model.Author, new { @class = "form-control", placehodel = "Author" })
            </td>
        </tr>
    </table>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
